"use client";
import { useState } from "react";
export default function ModalToggle({children}: Readonly<{children: React.ReactNode}>) {
    
    return (
        <>
            {children}
        </>
    )
}